name: plant_identifier
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.5.3 <4.0.0"  # Updated SDK constraint

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0  # For camera functionality
  image_picker: ^1.0.7  # Updated version
  tflite: ^1.1.2  # Correct TensorFlow Lite package
  logging: ^1.1.0  # For logging
  logger: ^2.0.2  # For better logging
  path_provider: ^2.1.1  # For saving images to permanent storage
  permission_handler: ^10.4.0  # For handling Android permissions
  flutter_map: ^6.1.0
  latlong2: ^0.9.0
  http: ^1.3.0
  get: ^4.6.5
  fl_chart: ^0.63.0
  shared_preferences: ^2.0.15
  intl: ^0.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0  # For linting

flutter:
  uses-material-design: true
  
    